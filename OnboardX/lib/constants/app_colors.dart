import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color background = Color(0xFF082257);
  static const Color accent = Color(0xFF5200FF);
  static const Color lightAccent = Color(0xFF8C4DFF);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color cardBackground = Color.fromRGBO(255, 255, 255, 0.15);
}
